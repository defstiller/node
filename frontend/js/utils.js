function getFormData(elements) {
	console.log(elements);
	// const data = elements.reduce((prev, current) => {
	// 	const { name, value } = current;
	// 	return { ...prev, [name]: value };
	// })
	// return data;
}
export { getFormData };